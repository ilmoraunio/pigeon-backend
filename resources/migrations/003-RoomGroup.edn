{:up ["CREATE TABLE RoomGroup (id SERIAL PRIMARY KEY,
                               Room_name varchar(1000),
                               name varchar(1000),
                               parent integer,
                               FOREIGN KEY(Room_name) REFERENCES Room(name),
                               UNIQUE(Room_name, name),
                               FOREIGN KEY(parent) REFERENCES RoomGroup(id));"]
 :down ["DROP TABLE RoomGroup;"]}