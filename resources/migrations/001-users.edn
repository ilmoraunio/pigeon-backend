{:up ["CREATE TABLE users (id int,
                           username text unique,
                           full_name text,
                           password text,
                           deleted boolean default false);"]
 :down ["DROP TABLE users;"]}
