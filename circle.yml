dependencies:
  pre:
    - git config --global user.email "circleci@ignore"
    - git config --global user.name "CircleCI"
test:
  override:
    - lein midje
deployment:
  production_deploy:
    branch: circle-automatic-master-deploy
    commands:
      - git push heroku circle-automatic-master-deploy:master